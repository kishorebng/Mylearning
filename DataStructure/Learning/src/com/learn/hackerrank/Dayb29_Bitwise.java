package com.learn.hackerrank;

import java.util.ArrayList;
import java.util.Collections;
import java.util.HashMap;
import java.util.List;
import java.util.Scanner;
import java.util.regex.Matcher;
import java.util.regex.Pattern;

/**
 * 
 * @author Kishore Problem Statement
 * 
 *         Given set . Find two integers, and (where ), from set such that the
 *         value of is the maximum possible and also less than a given integer,
 *         . In this case, represents the bitwise AND operator.
 * 
 *         Input Format
 * 
 *         The first line contains an integer, , the number of test cases. Each
 *         of the subsequent lines defines a test case as space-separated
 *         integers, and , respectively.
 * 
 *         Constraints
 * 
 *         Output Format
 * 
 *         For each test case, print the maximum possible value of on a new
 *         line.
 * 
 *         Sample Input
 * 
 *         3 5 2 8 5 2 2 Sample Output
 * 
 *         1 4 0 Explanation
 * 
 * 
 * 
 *         All possible values of and are:
 * 
 *         The maximum possible value of that is also is , so we print on a new
 *         line.
 */


public class Dayb29_Bitwise {
	
	    public static void main(String[] args) {
	        Scanner in = new Scanner(System.in);
	        int t = in.nextInt();
	        for(int a0 = 0; a0 < t; a0++){
	            int n = in.nextInt();
	            int k = in.nextInt();
	        }
	    }
}
